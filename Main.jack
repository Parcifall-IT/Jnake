class Main {

    function void main () {
        var Game game;
        var boolean restart;
        let game = Game.init(6);
        let restart = game.move();
        while(restart) {
        
            let restart = false;
            //do camera.dispose();
            //let camera = Camera.init(1);
            //do Screen.clearScreen();
            let restart = game.move();
        }
        return;
    }
}