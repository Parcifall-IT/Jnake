class Block {
    field int X, Y;
    static int size;

    constructor Block new(int x, int y) {
        let X = x;
        let Y = y;
        let size = 10;

        return this;
    }

    function void Draw(Block block) {
        var int x1, x2, y1, y2;
        let x1 = block.GetCoord(true);
        let x2 = x1 + size;
        let y1 = block.GetCoord(false);
        let y2 = y1 + size;
        do Screen.drawRectangle(x1, y1, x2, y2);

        return;
    }

    method int GetCoord (boolean coord) {
         if(coord) {
            return X; 
        }
        return Y;
    }

    method void ChangeCoords(int x, int y) {
        let X = x;
        let Y = y;

        return;
    }

    function void dispose(Block block) {
        do Memory.deAlloc(block);
        return;
    }
}